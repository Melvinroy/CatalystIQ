# CatalystIQ Frontend

React + Vite dashboard for the pre-market breakout scanner.

## Run

1. Install dependencies.
   - `npm install`
2. Copy `.env.example` to `.env` and set `VITE_API_BASE_URL`.
3. Start dev server.
   - `npm run dev`
