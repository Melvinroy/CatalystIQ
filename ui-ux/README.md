# UI/UX

Design artifacts for CatalystIQ.

## Subfolders

- `wireframes/` low/high fidelity screens
- `design-tokens/` color, type, spacing tokens
- `assets/` icons and static assets

Keep implementation-independent design files here.
