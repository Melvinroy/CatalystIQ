# Testing

Project-level test strategy and non-unit suites.

## Subfolders

- `e2e/` browser-based end-to-end tests
- `performance/` load/perf checks

Backend unit/integration tests remain in `backend/tests/`.
